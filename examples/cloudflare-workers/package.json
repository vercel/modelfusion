{
  "name": "modelfusion-cloudflare-workers",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc --outDir build/",
    "clean": "rimraf build dist .turbo node_modules && pnpm clear",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230419.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.22.1"
  },
  "dependencies": {
    "modelfusion": "0.105.0"
  }
}
